services:
  - type: web
    name: notes-app-server
    runtime: node
    rootDir: notes-app-server
    buildCommand: npm install && npm run build
    startCommand: node dist/index.js
    envVars:
      - key: DATABASE_URL
        sync: false